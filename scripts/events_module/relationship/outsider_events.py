
EXILED_CATS = {
    "cat_to": ['bumped into (cat) at the Pack border.', 'caught a glimpse of (cat) from the distance.'],
    "cat_from": ['was wandering near the Pack territory and met (cat).'],
    "both": ['ran into (cat) by chance.']
}

OUTSIDE_CATS = {
    "cat_to": ['is thinking about (cat).'],
    "cat_from": ['is thinking about (cat) as they wander far from Pack territory.'],
    "both": ['wonders where (cat) is right now.']
}
